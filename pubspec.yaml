name: session_insight_kit
description: "A Flutter package for collecting and analyzing session insights."
version: 0.0.1
repository: https://github.com/blendfactory/session-insight-kit
documentation: https://github.com/blendfactory/session-insight-kit#readme
issue_tracker: https://github.com/blendfactory/session-insight-kit/issues

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
